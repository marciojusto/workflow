# AC #2 — MMH-1435

**Ticket:** MMH-1435
**AC Index:** 1
**AC ID:** AC2
**Completion:** 50% (2/4 ACs)
**Timestamp:** 2026-04-16T21:45:53Z

---

## Acceptance Criteria

Given a simulation is displayed
When the Asset tab is opened
Then the VAT percentage provided by Miles is displayed
And the value is not editable

---

## Implementation Summary

- Created VatSettings MongoDB model with explicit collection name `vat_settings` and seed script
- Added `getVatPercentageFromMongo()` in mappingResponse.ts to lookup VAT percentage using `catalogVatCode.enumId` + `GATEWAY_COUNTRY`
- Added `CORE` to `COUNTRY_CODE_MAP` for country code resolution
- Added `vatPercentage` field to `PricingDetails` type
- Fixed missing `vatPercentage` mapping in `assetService.ts` (`mapToAssetPricingDetails`) and `useInit.ts` (pricingDetails construction on page reload)
- Fixed hardcoded `vatEnumId` in `assets.ts` to use `this.pricingDetails?.vat ?? "100002"`
- Added VAT percentage display row to `AssetSummary.vue` showing formatted percentage (e.g., `20%`)
- Added i18n translations for en-US and fr-FR
- Made `mappingCreateDealResponse` and `buildCalculateResponse` async to support MongoDB lookup

---

## Files Changed

- `server/models/VatSettings.ts` — New Mongoose model for VAT settings (collection `vat_settings`)
- `server/utils/mappingResponse.ts` — Added `getVatPercentageFromMongo()`, `CORE` to country map, async VAT lookup
- `types/types.ts` — Added `vatPercentage` to `PricingDetails`
- `features/asset/stores/assets.ts` — Normalization of `vatPercentage`, fixed hardcoded `vatEnumId`
- `features/asset/stores/services/assetService.ts` — Added `vatPercentage` to `mapToAssetPricingDetails`
- `composables/useInit.ts` — Added `vatPercentage` to pricingDetails construction
- `features/asset/components/demandAssetDetails/AssetSummary.vue` — VAT percentage display row
- `locales/en-US.json` — Translation "VAT percentage"
- `locales/fr-FR.json` — Translation "Taux TVA"
- `scripts/insertVatData.cjs` — Seed script for VAT settings
- `scripts/data/vat.csv` — CSV data with Enumvalue column

---

## Code Changes

### server/models/VatSettings.ts

```diff
+ export const VatSettings: Model<IVatSettings> =
+   mongoose.models.VatSettings || mongoose.model('VatSettings', VatSettingsSchema, 'vat_settings')
```

### server/utils/mappingResponse.ts

```diff
  const COUNTRY_CODE_MAP: Record<string, string> = {
    PT: "Portugal",
    IT: "Italy",
    ES: "Spain",
    NL: "Netherlands",
+   CORE: "CORE",
  };

+ async function getVatPercentageFromMongo(enumId: string | undefined, countryCode?: string): Promise<number | null> { ... }
```

### features/asset/stores/assets.ts

```diff
- const vatEnumId = "100002";
+ const vatEnumId = this.pricingDetails?.vat ?? "100002";
```

### features/asset/stores/services/assetService.ts

```diff
  pricingDetails: {
    vat: data["vatPercent"],
+   vatPercentage: data["vatPercentage"] ?? null,
    totalSalePrice: data["totalSalePrice"] ?? 0,
  },
```

### composables/useInit.ts

```diff
  vat: quoteCalc?.vatPercent ?? null,
+ vatPercentage: quoteCalc?.vatPercentage ?? null,
```

---

## Validation

- [x] Code usage verified (all imports, functions, variables in use)
- [x] Manual browser validation — VAT percentage displayed correctly (20%)
- [x] Bugs found and fixed: missing vatPercentage in assetService.ts and useInit.ts

---

## Bugs Fixed During Review

1. **VatSettings model** queried wrong collection (`vatsettings` vs `vat_settings`) — fixed by adding explicit collection name
2. **GATEWAY_COUNTRY="CORE"** not in COUNTRY_CODE_MAP — query ran without country filter — fixed by adding CORE mapping
3. **assetService.ts** `mapToAssetPricingDetails` omitted `vatPercentage` — VAT percentage lost on calculate calls
4. **useInit.ts** pricingDetails construction omitted `vatPercentage` — VAT percentage lost on page reload
